import { LandingDirective } from './landing.directive';

describe('LandingDirective', () => {
  it('should create an instance', () => {
    const directive = new LandingDirective();
    expect(directive).toBeTruthy();
  });
});
